name: Test

on:
  push:
    branches: ["*"]
  pull_request:
    branches: ["*"]
  
jobs:
  this_job_run_the_tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: astral-sh/setup-uv@v5
        name: install UV
      - run: echo "hola mundo"
      - run: ls -la